## What I Need

I need a dashboard with surreal db and tables needed to be visible. every project need to clone this project over and over
again. It need to have api so frontend can get data. Need to be authenticated to get data. dashboard should have 
special access to get information about tables in database. Dashboard should have a analysis panel with charts to analysis
the database. It must give public access to other application using backend as a service